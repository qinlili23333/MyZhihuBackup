在Android中，用返回键退出app相比直接划掉app，前者第二次打开会更快一些。不是因为前者是假退出，而是因为前者虽然销毁了activity，但保留了进程context，在下次打开时不需要zygote再次孵化进程，所以节约了时间。Android中的氟化进程相当需要时间，所以一般情况下，我不建议由子进程做ui进程，这会造成app表现为响应缓慢。一般应该由子进程做服务进程或监控进程，主进程做ui进程。